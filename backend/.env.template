# Railway will prompt you to fill these when you connect the repo
# Copy this template to Railway Variables tab

NODE_ENV=production
PORT=3000
DATABASE_URL=${{Postgres.DATABASE_URL}}
REDIS_URL=${{Redis.REDIS_URL}}
GEMINI_API_KEY=
JWT_SECRET=
FRONTEND_URL=${{frontend.RAILWAY_PUBLIC_DOMAIN}}
ALLOWED_ORIGINS=${{frontend.RAILWAY_PUBLIC_DOMAIN}}
