[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[phases.setup]
cmds = ["npx tsx scripts/ingest-from-all-sources.ts || true"]

[start]
cmd = "npm run dev"
