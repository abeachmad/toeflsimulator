[phases.setup]
nixPkgs = ["nodejs_22"]
cmds = ["npx tsx scripts/ingest-from-all-sources.ts || true"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run dev"
